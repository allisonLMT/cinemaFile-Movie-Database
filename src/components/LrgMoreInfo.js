//need to add the button and dynamically generate where the link goes to
import React from 'react';
import styles from '../styles/lrgMoreInfo.module.css';


function LrgMoreInfo() {
    return (
        <div className={styles.lrgMoreInfo}>
            <p>more info</p>
        </div>
    )
}

export default LrgMoreInfo;